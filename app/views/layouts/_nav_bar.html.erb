<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href=<%= root_path%>>
        <%= image_tag "logo-welcome.png", class: "logo" %>
      </a>
    </div>
    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li class=<%= 'active' if current_page?(root_path) %>><a href=<%= root_path %> >Inicio<span class="sr-only">(current)</span></a></li>
        <li class=><a href=<%= root_path(anchor: 'presentation') %> >Presentación</a></li>
        <li class=><a href=<%= root_path(anchor: 'professorate') %> >Profesorado</a></li>
        <li class=<%= 'active' if current_page?(topics_path) %>><a href=<%= topics_path %> >Temario</a></li>
      </ul>
        <ul class="nav navbar-nav navbar-right">
          <% if current_user.present?  %>
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Menú<span class="caret"></span></a>

              <ul class="dropdown-menu">
                <li><a href="">Editar mi perfil</a></li>
                <li><%= link_to "Cerrar sesión", destroy_user_session_path, method: :delete %></li>
                <% if can? :manage, :rails_admin  %>
                  <li><%= link_to "Administración", rails_admin_path %></li>
                <% end %>
              </ul>
            </li>
          <% else %>
            <li><%= link_to "Acceder", new_user_session_path %></li>
          <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div>
</nav>
